import React from 'react';
import Base from '../core/Base'
const UserDashBoard = () => {
    return (
        <Base title="User Dash Board Page">
            <h1>This is the User Page page</h1>
        </Base>
    );
}

export default UserDashBoard;
